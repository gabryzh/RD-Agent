# Qlib 初始化配置
qlib_init:
    # 数据存储路径
    provider_uri: "~/.qlib/qlib_data/cn_data"
    # 区域设置为中国
    region: cn

# 定义市场和基准的别名
market: &market csi300
benchmark: &benchmark SH000300

# 数据处理器配置
data_handler_config: &data_handler_config
    # 数据起止时间
    start_time: 2008-01-01
    end_time: 2022-08-01
    # 股票池
    instruments: *market
    # 数据加载器配置
    data_loader:
        # 使用嵌套数据加载器，可以合并来自不同来源的数据
        class: NestedDataLoader
        kwargs:
            # 定义多个数据加载器
            dataloader_l:
                # 第一个数据加载器：加载 Alpha158 因子
                - class: qlib.contrib.data.loader.Alpha158DL
                  kwargs:
                    config:
                        # 标签定义
                        label: 
                            - ["Ref($close, -2)/Ref($close, -1) - 1"]
                            - ["LABEL0"]
                        # 特征定义，这里列出了一些 Alpha158 的表达式和对应的名称
                        feature:
                            - ["Resi($close, 5)/$close", "Std(Abs($close/Ref($close, 1)-1)*$volume, 5)/(Mean(Abs($close/Ref($close, 1)-1)*$volume, 5)+1e-12)",
                               "Rsquare($close, 5)", "($high-$low)/$open", "Rsquare($close, 10)", "Corr($close, Log($volume+1), 5)",
                               "Corr($close/Ref($close,1), Log($volume/Ref($volume, 1)+1), 5)", "Corr($close, Log($volume+1), 10)",
                               "Ref($close, 60)/$close", "Resi($close, 10)/$close", "Std($volume, 5)/($volume+1e-12)",
                               "Rsquare($close, 60)", "Corr($close, Log($volume+1), 60)", "Std(Abs($close/Ref($close, 1)-1)*$volume, 60)/(Mean(Abs($close/Ref($close, 1)-1)*$volume, 60)+1e-12)",
                               "Std($close, 5)/$close", "Rsquare($close, 20)", "Corr($close/Ref($close,1), Log($volume/Ref($volume, 1)+1), 60)",
                               "Corr($close/Ref($close,1), Log($volume/Ref($volume, 1)+1), 10)", "Corr($close, Log($volume+1), 20)",
                               "(Less($open, $close)-$low)/$open"]
                            - ["RESI5", "WVMA5", "RSQR5", "KLEN", "RSQR10", "CORR5", "CORD5", "CORR10", 
                               "ROC60", "RESI10", "VSTD5", "RSQR60", "CORR60", "WVMA60", "STD5", 
                               "RSQR20", "CORD60", "CORD10", "CORR20", "KLOW"]
                # 第二个数据加载器：加载静态数据文件，即外部生成的组合因子
                - class: qlib.data.dataset.loader.StaticDataLoader
                  kwargs:
                    config: "combined_factors_df.parquet"

    # 学习阶段的数据处理器
    learn_processors:
        # 丢弃标签为空的数据
        - class: DropnaLabel
        # 截面 Z-Score 标准化
        - class: CSZScoreNorm
          kwargs:
              fields_group: label

# 投资组合分析配置 (与 baseline 配置相同)
port_analysis_config: &port_analysis_config
    strategy:
        class: TopkDropoutStrategy
        module_path: qlib.contrib.strategy
        kwargs:
            signal: <PRED>
            topk: 50
            n_drop: 5
    backtest:
        start_time: 2017-01-01
        end_time: 2020-08-01
        account: 100000000
        benchmark: *benchmark
        exchange_kwargs:
            limit_threshold: 0.095
            deal_price: close
            open_cost: 0.0005
            close_cost: 0.0015
            min_cost: 5

# 任务配置 (与 baseline 配置大部分相同)
task:
    model:
        class: LGBModel
        module_path: qlib.contrib.model.gbdt
        kwargs:
            loss: mse
            colsample_bytree: 0.8879
            learning_rate: 0.2
            subsample: 0.8789
            lambda_l1: 205.6999
            lambda_l2: 580.9768
            max_depth: 8
            num_leaves: 210
            num_threads: 20
    dataset:
        class: DatasetH
        module_path: qlib.data.dataset
        kwargs:
            handler:
                # 使用 DataHandlerLP，支持更灵活的数据处理流程
                class: DataHandlerLP
                module_path: qlib.contrib.data.handler
                kwargs: *data_handler_config
            segments:
                train: [2008-01-01, 2014-12-31]
                valid: [2015-01-01, 2016-12-31]
                test: [2017-01-01, 2020-08-01]
    # 记录器配置 (与 baseline 配置相同)
    record: 
        - class: SignalRecord
          module_path: qlib.workflow.record_temp
          kwargs: 
            model: <MODEL>
            dataset: <DATASET>
        - class: SigAnaRecord
          module_path: qlib.workflow.record_temp
          kwargs: 
            ana_long_short: False
            ann_scaler: 252
        - class: PortAnaRecord
          module_path: qlib.workflow.record_temp
          kwargs: 
            config: *port_analysis_config
